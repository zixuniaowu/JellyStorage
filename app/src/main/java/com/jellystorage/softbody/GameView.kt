package com.jellystorage.softbody

import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import com.jellystorage.play.PlayScreen

/**
 * Publishable entry: 果冻勇者 — title, run, map, combat, progress.
 */
@Composable
fun GameView(modifier: Modifier = Modifier) {
    PlayScreen(modifier = modifier)
}
